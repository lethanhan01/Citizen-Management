service: node-serverless-api

provider:
    name: aws
    runtime: nodejs14.x
    region: ap-southeast-1

functions:
    api:
        handler: src/handler.cjs.api
        events:
            - httpApi: "*"

plugins:
    - serverless-offline
